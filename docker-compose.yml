services:
  frontend:
    build: .
    container_name: fasealterna-frontend
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - reverse-proxy
      - fasealterna-network

networks:
  reverse-proxy:
    external: true
  fasealterna-network:
    external: true